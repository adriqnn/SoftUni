function echo_functionV1(input){
    return `${input.length}\n${input}`;
}

function echo_functionV2(input){
    let inputLength = input.length;
    
    return `${inputLength}\n${input}`;
}
