function smallest_of_three_numbersV1(number1, number2, number3){
    return Math.min(number1, number2, number3);
}

const smallest_of_three_numbersV2 = function(number1, number2, number3){
    return Math.min(number1, number2, number3);
}

const smallest_of_three_numbersV3 = (number1, number2, number3) => {
    return Math.min(number1, number2, number3);
}