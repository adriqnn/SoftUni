function nxn_matrixV1(number){
    let result = "";

    for (let i = 0; i < number; i++) {
        for (let j = 0; j < number; j++) {
            result += `${number} `;
        }

        result += `\n`;
    }

    return result;
}

const nxn_matrixV2 = function(number){
    let result = "";

    for (let i = 0; i < number; i++) {
        for (let j = 0; j < number; j++) {
            result += `${number} `;
        }

        result += `\n`;
    }

    return result;
}

const nxn_matrixV3 = (number) => {
    let result = "";

    for (let i = 0; i < number; i++) {
        for (let j = 0; j < number; j++) {
            result += `${number} `;
        }

        result += `\n`;
    }

    return result;
}
