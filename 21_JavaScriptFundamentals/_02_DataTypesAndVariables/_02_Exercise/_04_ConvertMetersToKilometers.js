function convert_meters_to_kilometersV1(number){
    return (number / 1000).toFixed(2);
}

const convert_meters_to_kilometersV2 = function(number){
    return (number / 1000).toFixed(2);
}

const convert_meters_to_kilometersV3 = (number) => {
    return (number / 1000).toFixed(2);
}