function binary_to_decimalV1(number){
    return parseInt(number, 2);
}

const binary_to_decimalV2 = function(number){
    return parseInt(number, 2);
}

const binary_to_decimalV3 = (number) => {
    return parseInt(number, 2);
}