function required_readingV1(book, pages, days){
    let pagesPerHour = book / pages;
    console.log(pagesPerHour / days);
}

const required_readingV2 = function(book, pages, days){
    let pagesPerHour = book / pages;
    console.log(pagesPerHour / days);
}

const required_readingV3 = (book, pages, days) => {
    let pagesPerHour = book / pages;
    console.log(pagesPerHour / days);
}
