function substringV1(word, start, end){
    return word.slice(start, start + end);
}

const substringV2 = function(word, start, end){
    return word.slice(start, start + end);
}

const substringV3 = (word, start, end) => {
    return word.slice(start, start + end);
}